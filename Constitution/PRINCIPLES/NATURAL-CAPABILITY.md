# Principle of Natural Capability

> VIMAX uses only the real capabilities of entities.

A capability must be explicitly declared, supported by the nature of the entity, bounded by known limitations, and verified by observation, specification, or test.

A temperature sensor may provide temperature data. It must not be treated as a pressure sensor unless pressure measurement is a real, declared, and verified capability.
